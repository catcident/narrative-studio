# Railway Nixpacks configuration
[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["cd demo && npm install"]

[phases.build]
cmds = ["cd demo && npm run build"]

[start]
cmd = "cd demo && npm run start"
