# Next.js
.next/

# Build output (Next.js standalone)
dist/

# Environment variables (보안)
# .env는 공개 가능한 기본값만 포함 (커밋 허용)
.env.local
.env.*.local

# Docker Compose overrides (개발 환경용)
docker-compose.override.yml
