# 기본 설정 (민감하지 않은 정보)
# 민감한 정보는 .env.local에 설정하세요
NODE_ENV=production
PORT=3000

# 인증 모드 (기본: 비활성 — 인증이 필요하면 .env.local 또는 환경변수로 설정)
# AUTH_ENABLED=true 는 .env.local에서 설정
# Docker 프로덕션 빌드는 Dockerfile에서 설정

# Catcident OAuth (공개 정보)
AUTH_CATCIDENT_ISSUER=https://catcident.com
