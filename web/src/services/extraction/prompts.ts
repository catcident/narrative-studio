/**
 * 지식 그래프 추출 서비스 — LLM 프롬프트 상수
 */

export const USER_PROMPT = `소설 텍스트 청크 {{chunkNum}} 분석

{{text}}

---
{{previousCharacters}}

## 출력 형식 (JSON)
\`\`\`json
{
  "chapters": [{"id": 1, "title": "텍스트에서 찾은 실제 장/화 제목 (예: '3화: 박스')"}],
  "scenes": [{
    "id": 1,
    "chapter": 1,
    "location": "장소",
    "time": "시간(있으면)",
    "summary": "장면 요약",
    "time_marker": "텍스트에 명시된 시간 표현만 (예: '10년 전', '다음 날', '회귀 전') - 없으면 null"
  }],
  "entities": [{
    "name": "이름",
    "category": "character|location|item|concept",
    "description": "핵심 특성/역할",
    "scenes": [1],
    "aliases": ["별칭"]
  }],
  "relationships": [{
    "from": "A",
    "to": "B",
    "type": "관계타입",
    "description": "관계 설명",
    "scenes": [1]
  }]
}
\`\`\`

## 핵심 규칙

### 1. 장/화 인식 (매우 중요!)
텍스트에서 장/화/에피소드 구분을 찾아 chapters에 기록합니다.

**인식해야 할 패턴들:**
- 마크다운: "# 1화: 아침", "# 3화: 박스", "## 제1장: 시작"
- 숫자+단위: "1화", "2장", "제3장", "4편", "5절"
- 콜론/공백 형식: "1화:", "1화 제목", "제1장: 시작"
- 괄호 형식: "[1화]", "【제1장】", "(Episode 1)"
- 영문: "Chapter 1", "Episode 1", "Part 1", "Ch.1", "Ep.1"
- 특수: "프롤로그", "에필로그", "Prologue", "Epilogue", "서장", "종장", "막간"

**⚠️ 제목은 텍스트에서 그대로 복사하세요!**
- 텍스트에 "# 3화: 박스"가 있으면 → title: "3화: 박스"
- 텍스트에 "# 7화: 거꿀"이 있으면 → title: "7화: 거꿀"
- 예시 형식("1화: 제목")을 그대로 쓰지 마세요. 실제 텍스트의 제목을 사용하세요!

### 2. 장면 분할 및 시간 처리
시간/장소 변경 시 새 장면.

**시간 기록 원칙 (중요!):**
- time_marker에는 텍스트에 **명시적으로 적힌 시간 표현만** 기록
- 예: "10년 전", "다음 날", "그로부터 일주일 후", "회귀 전", "어린 시절"
- **추측하지 마세요!** 단서가 없으면 null로 두세요
- 이게 과거인지 현재인지 **판단하려 하지 마세요** - 나중에 후처리로 계산합니다

### 3. 엔티티 추출 - 전수 추출 원칙
**모든 것을 추출**합니다. 중요도는 후처리에서 계산합니다.

**⚠️ 대명사/지시어 처리 (매우 중요!):**

대명사("나", "그", "그녀", "그들")나 지시어("이 남자", "그 여자", "저 아이")는 엔티티 이름으로 사용하지 마세요!
반드시 문맥에서 누구를 가리키는지 파악하여 실제 이름/명칭으로 해석(resolve)하세요.

**1인칭 (나/저/우리):**
- 이름이 밝혀지면 그 이름 사용: "나는 서준이다" → name: "서준"
- 단서가 있으면 설명적 이름: "나는 카페를 운영한다" → name: "카페 주인"
- 아무 단서도 없으면 "나" 허용, 단 화자가 바뀌면 반드시 구분
  - 예: 1화 "서준"(aliases: ["나"]), 2화 "수아"(aliases: ["나"])

**3인칭 (그/그녀/그들/그 사람):**
- "그"가 누구인지 문맥에서 파악하여 해당 캐릭터 이름 사용
  - 예: "민수가 돌아섰다. 그는 웃었다." → "그"는 민수 → 민수의 scenes에 추가
- **절대 "그", "그녀", "그 남자" 등을 엔티티 이름으로 만들지 마세요**
- 누구인지 파악 불가능하면 해당 문장은 무시 (엔티티 추출 안 함)

**지시어 (이/저/그 + 명사):**
- "그 집" → 이전에 추출된 집 엔티티로 통합
- "저 남자" → 문맥에서 해당 캐릭터 파악하여 기존 엔티티로 통합
- "이것", "저것" → 문맥에서 해당 아이템 파악

**카테고리 구분 기준:**
- **character**: 대화/행동하는 존재 (사람, 동물, 의인화된 것. 이름 없으면 "검은 고양이", "흰 수염 노인" 등 특징으로 완전한 명칭 사용)
- **location**: 공간/장소 (골목, 집, 방, 공원 등 - 캐릭터가 "~에 있다"고 할 수 있는 곳)
- **item**: 물건/도구 (쓰레기통, 박스, 음식, 책 등 - 이동/소유 가능한 것)
- **concept**: 추상적 개념, 세계관 설정, 현상, 능력 체계, 조직/단체 등
- **⚠️ organization(조직/단체/기관)은 concept으로 분류하세요** (예: "특수국", "A반", "축구부" → concept)

**⚠️ 이름 작성 시 주의:**
- 한 글자 이름은 피하세요! ("검" ❌ → "검은 고양이" ✓, "흰" ❌ → "흰 고양이" ✓)
- 색깔/특징만 있는 경우 "색깔 + 대상 종류" 형식 사용 (예: "검은 고양이", "붉은 여우", "하얀 강아지")
- 이름이 텍스트에 명시되어 있으면 그 이름 사용, 없으면 특징을 조합한 완전한 명칭 사용

**LLM이 중요도를 판단하지 마세요.** 일회성처럼 보여도 추출합니다.

### 4. 관계 타입 (10가지만 사용)
가족, 연인, 친구, 적대, 동료, 소속, 위치, 소유, 포함, 관련

**관계 타입 선택 기준 (정확히 지켜주세요!):**
- **가족**: 혈연, 결혼, 양육, 입양 등 모든 가족/친족 관계. description에 구체적 관계 명시 (예: "아버지-딸", "할머니-손녀", "부부", "형제", "양부")
- **연인**: 호감, 짝사랑, 고백, 연애, 로맨스 관계. 아직 사귀지 않아도 명확한 호감이면 연인
- **친구**: 대등한 우정, 동창, 소꿉친구
- **적대**: 원수, 라이벌, 적대 세력 간 대립
- **동료**: 같은 조직에서 **대등한 위치**의 구성원 간 관계만. description에 맥락 기록
- **소속**: 인물→조직/그룹. description에 직위나 역할 기록
- **위치**: 캐릭터/아이템이 특정 장소에서 활동하거나 존재. **장소(location) 엔티티를 추출했으면 반드시 관련 캐릭터와 "위치" 관계를 만드세요!** 장소가 관계 없이 떠있으면 안 됩니다
- **소유**: 인물이 물건을 소유
- **포함**: 하위 조직→상위 조직, 부분→전체
- **관련**: 위 9가지 중 어디에도 해당하지 않을 때만 사용. **최후의 수단!**

**⚠️ 관계 타입 주의사항:**
- 상하 관계(스승-제자, 상사-부하, 주인-종, 장문인-제자)는 "동료"가 아님! → "소속"으로 하되 description에 상하 관계 명시 (예: "스승-제자", "상사-부하")
- 텍스트에 명시적으로 가족임이 언급되면 반드시 "가족" 사용 (예: "~의 어머니", "~의 아들")
- "관련"을 사용하기 전에 다른 9가지 타입 중 적합한 것이 없는지 반드시 재검토

### 5. 엔티티 구분 vs 통일 (매우 중요!)

**⚠️ 이전에 추출된 엔티티 목록을 반드시 확인하세요!**

**같은 것 = 이전 이름 그대로 사용:**
- 같은 물건/장소/인물이 재등장 → 이전 이름 사용
- 같은 현상/개념의 다른 표현 → 이전 이름 사용
- 같은 장소의 다른 표현 → 이전 이름 사용 (예: "그 집", "낡은 집" → 같은 곳이면 "집"으로 통일)
- **⚠️ 캐릭터 이름이 밝혀지면 반드시 기존 엔티티에 병합!**
  - 텍스트에서 "그 고양이의 이름은 거꿀이다" 같은 문장이 나오면:
  - → 새로 "거꿀" 엔티티를 만들지 말고, 기존 "회색 고양이"의 aliases에 "거꿀" 추가
  - → name은 기존 것 유지, 새 이름은 aliases에!

**다른 것 = 새로 추출:**
- **⚠️ 다른 캐릭터는 절대 병합하지 마세요!** 이름이 비슷해도 다른 사람이면 별개 엔티티
  - 같은 성(姓)을 가진 가족이라도 각각 별개 엔티티 (예: 형제, 쌍둥이, 부모-자녀)
  - 텍스트에서 "A와 B"처럼 두 사람으로 언급되면 별개
  - 확실히 동일인이라는 증거가 없으면 병합하지 마세요
- **서사적 역할이 다른 물건**: 평범한 도구 vs 특별한 능력을 가진 물건 → 별개
- **물리적으로 다른 객체**: 같은 종류여도 별개 개체면 별개
- **같은 종류지만 용도/역할이 다른 물건**: 이름이 비슷해도 별개로 추출

**판단 기준**: "텍스트에서 이 둘이 동일한 하나의 존재로 취급되는가?"
- YES → 이전 이름 사용 + aliases에 추가
- NO 또는 불확실 → 별개 엔티티로 추출

### 6. 관계 필수 규칙
- from/to는 반드시 entities의 name과 동일해야 함
- 같은 장면에 등장하는 캐릭터들 사이에 관계 추출
- 텍스트에서 "A의 아버지 B", "B의 딸 C" 등 명시적 가족 언급이 있으면 반드시 관계로 추출
- 같은 관계를 양방향으로 중복 생성하지 마세요 (A→B만 만들고, B→A는 만들지 않음)
- **⚠️ 모든 엔티티는 최소 1개의 관계가 있어야 합니다!** 관계 없이 떠있는 엔티티가 없도록 하세요. 특히 location 엔티티는 해당 장소에서 활동하는 캐릭터와 "위치" 관계를 반드시 만드세요

### 7. JSON 출력 규칙 (매우 중요!)
- **반드시 위의 JSON 형식만** 출력하세요. 설명이나 코멘트 없이 JSON만!
- JSON 앞뒤에 \`\`\`json ... \`\`\` 마크다운 코드블록을 사용하세요
- 모든 문자열 값은 큰따옴표(")로 감싸세요
- 배열의 마지막 항목 뒤에 쉼표(,)를 넣지 마세요 (trailing comma 금지)
- description에 큰따옴표가 포함되면 \\"로 이스케이프하세요
- **절대로 데이터를 반복하지 마세요** (같은 엔티티/관계를 여러 번 출력 금지)`;

// 엔티티 선별용 프롬프트
export const ENTITY_SELECTION_PROMPT = `다음 텍스트를 분석할 예정입니다. 아래 텍스트 미리보기를 읽고, 기존 엔티티 목록에서 이 텍스트와 **관련 있는 것만** 선택하세요.

## 텍스트 미리보기 (앞부분 1000자)
{{textPreview}}

---

## 기존 엔티티 목록
각 엔티티의 이름, 카테고리, 설명, 연결된 관계가 표시됩니다.
이 중에서 위 텍스트에 직접 언급되거나, 맥락상 관련 있는 엔티티만 선택하세요.

{{entitySummaries}}

---

## 지시사항
1. 텍스트에 직접 이름이 언급된 엔티티 선택
2. 텍스트 내용과 관련된 엔티티 선택 (예: "집에서" → "집" 선택)
3. 선택된 엔티티와 밀접하게 연결된 것도 고려 (소유자, 가족 등)
4. **concept 타입 (세계관, 현상, 규칙 등)은 관련 내용이 언급되면 반드시 선택** (예: "시간이 거꾸로" → "시간의 역전" 선택)
5. 무관한 엔티티는 제외

관련 있는 엔티티의 이름만 JSON 배열로 반환하세요.
예: ["나", "박스", "집"]

⚠️ JSON 배열만 반환. 설명 없이 이름 목록만.

**주의: 누락 방지**
- 주인공(화자/나)이 텍스트에 나오면 반드시 선택
- 주요 인물의 소유물/관련 장소도 함께 선택
- 확실하지 않으면 포함하는 쪽으로 선택 (빠뜨리는 것보다 과선택이 나음)`;

// 엔티티 병합 검토용 프롬프트
export const ENTITY_MERGE_REVIEW_PROMPT = `다음은 소설에서 추출된 엔티티(등장인물, 장소, 물건 등) 목록입니다.
이 중에서 **같은 대상인데 다른 이름으로 추출된 것**을 찾아 병합 제안을 해주세요.

## 엔티티 목록
{{entityList}}

---

## 지시사항
1. 같은 캐릭터/장소/물건인데 다른 이름으로 추출된 것을 찾으세요
   - 예: "검은 고양이"와 "거꿀"이 같은 고양이라면 병합
   - 예: "집"과 "오래된 집"이 같은 장소라면 병합
2. 별칭/호칭 관계를 찾으세요
   - 예: "김철수"와 "철수 형"은 같은 인물
3. **⚠️ 다른 대상은 절대 병합하지 마세요!**
   - 같은 성(姓)이라도 다른 인물은 별개 (가족/형제/쌍둥이 등)
   - "나"와 다른 캐릭터를 함부로 병합하지 마세요
   - 1인칭 화자("나")가 여러 명일 수 있습니다 (시점 변경)
   - 같은 종류지만 다른 개체는 병합하지 마세요
   - **확실하지 않으면 병합하지 마세요** (잘못된 병합이 누락보다 더 위험합니다)
4. 카테고리가 다른 것은 병합하지 마세요 (character와 item은 별개)

## 응답 형식 (JSON 배열)
\`\`\`json
[
  {
    "keep": "유지할 이름 (더 구체적이거나 자주 사용되는 이름)",
    "merge": "병합할 이름 (keep에 흡수될 이름)",
    "reason": "병합 이유 (한 줄)"
  }
]
\`\`\`

병합할 것이 없으면 빈 배열 \`[]\`을 반환하세요.
⚠️ JSON만 반환. 설명 없이.`;

// 로어북 추출용 프롬프트 (LLM B — 인물 프로필, 세계관, 대사 추출)
export const LOREBOOK_EXTRACTION_PROMPT = `소설 텍스트 청크 {{chunkNum}} — 로어북 정보 추출

{{text}}

---
{{previousEntities}}

## 출력 형식 (JSON)
\`\`\`json
{
  "scenes": [{"id": 1, "summary": "장면 한줄 요약"}],
  "lore_entries": [
    {
      "entity_name": "인물/장소/아이템 이름",
      "category": "카테고리",
      "content": "텍스트에서 확인된 구체적 정보",
      "quote": "관련 원문 인용 (대사나 묘사)",
      "scene": 1
    }
  ]
}
\`\`\`

## 장면 분할
시간/장소 변경 시 새 장면. 간단한 요약만 기록.

## 카테고리 (해당하는 것만 추출)

### 인물 관련
- **appearance**: 외모, 체형, 얼굴, 머리 스타일, 흉터, 나이대 등 신체적 특징
- **outfit**: 복장, 장비, 무기, 소지품, 착용하고 있는 것
- **personality**: 성격, 말투, 습관, 행동 패턴
- **ability**: 능력, 기술, 특기, 무공, 마법, 직업적 기술 등
- **background**: 배경, 출신, 과거, 경력
- **motivation**: 목표, 동기, 욕구, 두려움
- **relationship_detail**: 다른 인물과의 관계 상세 ("A를 형이라 부른다", "B를 경계한다")
- **quote**: 인물의 중요한 대사 (content에 대사의 맥락, quote에 원문)

### 세계관 관련
- **world_setting**: 세계의 규칙, 체계, 법칙, 제도, 문화, 풍습, 시대 배경
- **location_detail**: 장소의 상세 묘사, 지리, 분위기, 특징
- **organization_detail**: 조직, 세력, 단체의 구조, 규모, 목적, 규칙
- **item_detail**: 중요 아이템의 특수 능력, 유래, 사용법
- **event**: 중요 사건, 상태 변화, 전환점

## 핵심 규칙

### 1. 텍스트에 명시된 정보만 추출
**추측하지 마세요!** 텍스트에 직접 쓰여 있거나 명확히 추론 가능한 정보만 기록합니다.
- ✅ "검은 머리를 뒤로 묶고 있었다" → appearance: "검은 머리를 뒤로 묶은 스타일"
- ❌ "아마 전사일 것이다" → 추측이므로 추출하지 않음

### 2. 장면 단위로 기록
같은 인물이라도 장면마다 달라질 수 있는 정보(복장, 외모 변화)는 각각 기록합니다.
- 1장면: outfit "검은 코트" / 5장면: outfit "흰 셔츠로 갈아입었다"
- 둘 다 기록! 변화 추적이 목적입니다.

### 3. 이전 엔티티 이름 재사용
이전 청크에서 추출된 이름이 있으면 반드시 같은 이름을 사용하세요.

### 4. 대명사 해석
"나", "그", "그녀" 등 대명사는 실제 이름으로 해석하세요. 이름을 알 수 없으면 해당 엔트리를 생략합니다.

### 5. 중요한 대사 추출 (quote 카테고리)
인물의 성격, 목표, 관계를 드러내는 핵심 대사를 추출합니다.
- content에는 대사의 맥락/의미 설명
- quote에는 원문 대사를 그대로 인용
- 일상적인 대사("안녕", "고마워")는 제외, 캐릭터를 드러내는 대사만

### 6. 없으면 빈 배열
해당 청크에 뽑을 정보가 없으면 lore_entries를 빈 배열로 반환합니다.

### 7. JSON 출력 규칙
- 반드시 위의 JSON 형식만 출력. 설명이나 코멘트 없이 JSON만!
- JSON 앞뒤에 \`\`\`json ... \`\`\` 코드블록 사용
- trailing comma 금지
- 데이터 반복 금지`;
